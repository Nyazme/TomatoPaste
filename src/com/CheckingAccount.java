package com;
//again should probably read .example.java
//trying to setup the checking account for deposits/withdrawals w/overdraft

import com.company.BankAccount;

public class CheckingAccount extends BankAccount {
    public class CheckingAccount(int balance);
    public class Super(balance);

    @Override
    public void deposit (int balance)
this.balance=this balance + (deposit);
this.balance=this balance - (withdrawal);

}
